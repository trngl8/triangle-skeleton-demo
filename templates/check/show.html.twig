{% extends 'check/index.html.twig' %}

{% block main %}
    <h1 class="mb-4">{{ 'title.topic.show'|trans }} #{{ item.id }}</h1>

    <div class="row">
        <div class="col-md-6">
            <h2>{{ item.title }}</h2>
        </div>
    </div>

{% endblock %}
