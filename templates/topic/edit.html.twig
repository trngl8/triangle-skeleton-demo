{% extends 'topic/index.html.twig' %}

{% block title %}{{ 'title.topic.add'|trans }}{% endblock %}

{% block main %}
    <h1 class="mb-4">{{ 'title.topic.add'|trans }}</h1>

    <div class="row">
        <div class="col-md-6">
            <p>Topic</p>
            {{ form(form) }}
        </div>
    </div>

{% endblock %}