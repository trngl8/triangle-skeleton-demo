{% extends 'check/index.html.twig' %}

{% block main %}
    <h1 class="mb-4">{{ 'title.topic.run'|trans }} #{{ item.id }}</h1>

    <div class="row">
        <div class="col-md-6">
            <p>{{ item.description }}</p>
        </div>
    </div>

{% endblock %}
