{% extends 'offer/admin/index.html.twig' %}

{% block main %}
    <div class="row">
        <div class="col-md-8">
            <div class="card">
                <div class="card-body">
                    {{ form(form,{'attr': {'novalidate': 'novalidate'}}) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
