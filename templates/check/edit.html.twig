{% extends 'topic/index.html.twig' %}

{% block title %}{{ 'title.check.add'|trans }}{% endblock %}

{% block main %}
    <h1 class="mb-4">{{ 'title.check.add'|trans }}</h1>

    <div class="row">
        <div class="col-md-6">
            {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
                {{ form_widget(form) }}
                <button type="submit" class="btn-primary btn">{{ 'button.submit'|trans }}</button>
            {{ form_end(form) }}
        </div>
    </div>

{% endblock %}
