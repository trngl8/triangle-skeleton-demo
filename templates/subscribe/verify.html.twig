{% extends 'subscribe/index.html.twig' %}

{% block main %}
    <h1 class="mb-4">{{ 'title.verify'|trans }}</h1>

    <div class="row">
        <div class="col-md-6">
            {% block form %}
                {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
                {{ form_widget(form) }}
                {{ form_end(form) }}
            {% endblock %}
        </div>
    </div>
{% endblock %}
