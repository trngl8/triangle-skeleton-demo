{% extends 'check/index.html.twig' %}

{% block main %}
    <h1 class="mb-4">{{ 'title.topic.run'|trans }} #{{ item.id }}</h1>

    <div class="row">
        <div class="col-md-6">

{#            <p>{{ item.description }}</p>#}

            {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
                {{ form_widget(form) }}
                <button type="submit" class="btn-primary btn">{{ 'button.submit'|trans }}</button>
            {{ form_end(form) }}

        </div>
    </div>

{% endblock %}
